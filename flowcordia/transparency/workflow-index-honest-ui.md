# Honest UI rule

The page never converts missing connection, pending indexing, failed synchronization, invalid workflow, or exact-source mismatch into an empty successful graph. Each condition has a distinct visible state and a real recovery action where available.
