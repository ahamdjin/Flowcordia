# Workflow index fixture plan

Focused tests should cover a valid multi-node workflow, an invalid JSON document, an invalid canonical document, an empty workflow directory, a deleted workflow, a moved commit, a blob mismatch, a truncated tree, a rate-limited GitHub call, a duplicate push delivery, and a stale worker completion.
