[phase] apply recovery hardening
apps/webapp/app/features/flowcordia/operations/database-recovery.server.ts: expected one anchor, found 0: 'function parseMigrationOutput(output: string): string[] {\n  const migrations = output\n    .split(/\r?\n/)\n    .map((line) => line.trim())\n    .filter(Boolean);\n  if (\n    migrations.'
