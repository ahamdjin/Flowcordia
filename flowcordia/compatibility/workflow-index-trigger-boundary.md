# Trigger.dev compatibility boundary

This slice reuses existing organization, project, GitHub App, connected repository, authorization, logging, UI, and process-signal infrastructure. It does not alter deployment records, background workers, task runs, run queues, run engine snapshots, supervisor workloads, CLI behavior, Docker topology, or customer execution paths.
