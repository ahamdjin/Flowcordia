# Workflow index database constraints

The database enforces allowed lifecycle states, nonnegative counts, count totals, canonical workflow IDs, bounded safe paths, object-ID formats, valid-versus-invalid field combinations, unique project/repository paths and workflow IDs, delivery payload hashes, and cross-schema identity foreign keys.
