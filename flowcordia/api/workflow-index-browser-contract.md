# Browser contract

The workflow list loader returns repository owner/name/branch, normalized synchronization status, public workflow metadata, selected workflow ID, verified graph data, normalized load failure, and stale state. The command returns only success, exact commit, and counts or a normalized error. No internal storage or security coordinates cross the boundary.
