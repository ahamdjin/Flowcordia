# Dependency remediation boundary

## Result

The first complete OSV scan of the root pnpm workspace reported 120 vulnerable package occurrences, including 69 high-or-critical occurrences. This remediation updates the direct selections and transitive resolution boundaries that can be changed safely before Private Beta and reduces the exception-aware current-lockfile scan to zero unreviewed findings. The follow-up Turbo 2 migration removes two of the original nine temporary exceptions.

The remediation is not a blanket package refresh. It preserves the repository's three-day minimum release age, keeps the existing patched dependencies, and requires the complete repository build and test matrix before merge.

## Maintained dependency changes

- `@kubernetes/client-node` moves from the old `0.20.x` line to `1.4.0`. The provider source is migrated to the maintained request-object API and direct response bodies. Provider typechecking is a dedicated blocking gate.
- Remix packages used by the webapp move from `2.17.4` to `2.17.5`.
- DOMPurify moves to `3.4.12`.
- Bounded pnpm overrides select fixed releases for affected Vite, Webpack, `file-type`, `body-parser`, DOMPurify, `fast-uri`, Hono, Fastify static serving and Remix router dependency paths.
- Turborepo moves deliberately to `2.10.6` after its 72-hour release-age hold. The root configuration is migrated from the removed `pipeline` key to the Turbo 2 `tasks` schema, and repository generation plus a dry build graph are permanent blocking checks.
- OpenTelemetry stable packages remain aligned on their compatible release families. Forcing only `@opentelemetry/core` to 2.8 while `sdk-trace-base` remained on 1.30 caused the webapp to fail at startup; the incompatible override is prohibited and the complete SDK 2 migration remains explicit work. After restoring the compatible family, the production webapp build and complete webapp E2E suite pass.

The lockfile is generated by pnpm. It is never edited manually.

## Reviewed exceptions

All exceptions live in `osv-scanner.toml`, identify exact GHSA advisories, contain a specific reason and have an expiry date. The permanent policy gate rejects duplicate IDs, unexpected additions, missing reasons, expired entries, entries lasting more than 90 days and broad package-level exceptions.

| Advisory | Affected boundary | Current decision | Owner | Expiry |
| --- | --- | --- | --- | --- |
| `GHSA-866g-f22w-33x8` | `@ai-sdk/provider-utils` 1.x in core development/type-test tooling | No fixed compatible 1.x release exists. Resolve during the AI SDK compatibility upgrade. | Flowcordia maintainers | 2026-09-01 |
| `GHSA-mh99-v99m-4gvg` | `brace-expansion` copies in build/test tooling | Fixed release `5.0.8` was still inside the repository's 72-hour release-age hold when reviewed. Upgrade immediately after the hold and exception expire. | Flowcordia maintainers | 2026-08-03 |
| `GHSA-2j2x-hqr9-3h42` | React Router 6 through `@remix-run/testing` | Published remediation requires an incompatible router major. Resolve with the Remix framework upgrade. | Flowcordia maintainers | 2026-09-01 |
| `GHSA-337j-9hxr-rhxg` | React Router 6 through `@remix-run/testing` | Published remediation requires an incompatible router major. Resolve with the Remix framework upgrade. | Flowcordia maintainers | 2026-09-01 |
| `GHSA-9jcx-v3wj-wh4m` | React Router 6 through `@remix-run/testing` | Published remediation requires an incompatible router major. Resolve with the Remix framework upgrade. | Flowcordia maintainers | 2026-09-01 |
| `GHSA-wrjc-x8rr-h8h6` | React Router 6 through `@remix-run/testing` | Published remediation requires an incompatible router major. Resolve with the Remix framework upgrade. | Flowcordia maintainers | 2026-09-01 |
| `GHSA-8988-4f7v-96qf` | OpenTelemetry core 1.30 in the stable SDK 1.30 family | The patched core 2.8 release cannot be mixed into SDK 1.30. Keep Node's 16 KiB header cap, reject custom untrusted non-HTTP baggage extraction and complete the SDK 2 migration as one compatible unit. | Flowcordia maintainers | 2026-08-31 |

An exception is temporary risk ownership, not a statement that the advisory is harmless. Any change that makes an excepted package reachable through a new transport or removes a recorded workaround requires immediate reassessment rather than waiting for the expiry.

## Enforced checks

Every relevant pull request now runs:

1. raw OSV comparison between the base and head lockfiles, so a new occurrence cannot hide behind the existing exception file;
2. an exception-aware full scan of the proposed head lockfile;
3. strict TOML exception validation using Python's standard `tomllib` parser;
4. exact dependency-selection assertions, including the reviewed Turbo `2.10.6` direct selection, Turbo 2 schema, and prohibition of transitive Turbo overrides;
5. enforced OpenTelemetry 1.30 workarounds: no raised Node header-size flag and no custom W3C baggage propagator in application source;
6. database generation/build and Kubernetes provider typechecking against the maintained client;
7. repository generation and dry build-graph validation through Turbo 2;
8. the repository's normal formatting, lint, TypeScript, build, E2E and unit matrix.

Scheduled and `main` OSV scans use the same reviewed configuration and fail when an exception expires or any additional vulnerability appears.

## Evidence interpretation

The baseline counts came from the first root-lockfile OSV campaign run. The permanent source of truth is the checked-in lockfile plus current scanner artifacts, not the historical count. When advisory data changes, the current OSV run supersedes the original baseline. The final graph must prove Turbo 2 generation and build-graph compatibility, OpenTelemetry 1.30 startup compatibility and Kubernetes client 1.4 type compatibility before this record is accepted.

The compatibility strategy is intentionally split: this PR removes safely remediable findings while incompatible AI SDK, Remix router and OpenTelemetry major migrations remain explicit, expiring work instead of being disguised as transitive overrides.

## Honest limit

This remediation proves the checked lockfile has no OSV finding outside the seven explicit, expiring exceptions at scan time. It does not prove that advisory databases are complete, that every vulnerable function is reachable, that no unknown vulnerability exists, or that static dependency analysis replaces runtime security testing and incident response.
