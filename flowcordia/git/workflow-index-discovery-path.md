# Canonical discovery path

Only files directly under `.flowcordia/workflows` whose filename is a valid canonical workflow ID plus `.json` are indexed. Nested files, invalid IDs, and unrelated repository content are ignored. The catalog is sorted by path for deterministic processing.
