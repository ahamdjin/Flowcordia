# Read-only canvas boundary

Node buttons select an inspector only. They do not mutate position, configuration, runtime policy, code references, or edges. The page contains no save, create, delete, deploy, or run action. The synchronization action refreshes repository projection; it does not write Git content.
