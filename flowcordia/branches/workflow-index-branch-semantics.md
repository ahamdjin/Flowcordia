# Tracked branch semantics

The index follows the project's configured production branch. A push event schedules only when its verified branch exactly matches that mapping. Manual synchronization resolves the same server-owned branch at request time. The UI cannot switch branches in this slice.
