# Workflow index feature gates

The visible workflow page uses the same default-off global and organization Studio access checks as the proposal workspace. Background push processing is durable, while active worker consumption uses the existing default-off Flowcordia operations flag. Disabling Studio removes the surface; disabling the worker stops claims without deleting queued or indexed state.
