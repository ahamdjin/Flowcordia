# Workflow Studio permissions

GitHub read permission plus Studio feature access is required to list and inspect workflows. GitHub write permission plus Studio feature access is required to synchronize. Synchronization does not write GitHub, but the stronger permission matches the existing governance rule that repository lifecycle commands belong to repository writers.
