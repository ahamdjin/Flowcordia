# Stacked parent

This slice is intentionally based on PR #10, `agent/flowcordia-proposal-workspace`. It reuses the established Studio feature gate, dashboard authorization, connected repository resolver, proposal operations lifecycle, and proposal navigation while preserving its own review boundary.
