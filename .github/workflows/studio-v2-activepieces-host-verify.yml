name: Verify Studio V2 Activepieces host

on:
  push:
    branches:
      - agent/studio-v2-activepieces-frontend
    paths:
      - .github/workflows/studio-v2-activepieces-host-verify.yml

permissions:
  contents: write

jobs:
  verify:
    runs-on: ubuntu-latest
    timeout-minutes: 45
    steps:
      - uses: actions/checkout@v4
        with:
          ref: agent/studio-v2-activepieces-frontend
          fetch-depth: 0
      - uses: pnpm/action-setup@v4
        with:
          version: 10.33.2
      - uses: actions/setup-node@v4
        with:
          node-version: 20
      - name: Typecheck, test, and build host
        shell: bash
        run: |
          set +e
          set -o pipefail
          {
            set -euo pipefail
            pnpm install --no-frozen-lockfile --config.minimum-release-age=0
            pnpm --filter @flowcordia/studio-activepieces typecheck
            pnpm --filter @flowcordia/studio-activepieces test
            pnpm --filter @flowcordia/studio-activepieces build
            test -f apps/flowcordia-studio-activepieces/dist/index.html
          } 2>&1 | tee /tmp/flowcordia-activepieces-host.log
          status=${PIPESTATUS[0]}

          git config user.name "Flowcordia Studio Agent"
          git config user.email "actions@users.noreply.github.com"
          mkdir -p .flowcordia/automation
          if [ "$status" -eq 0 ]; then
            printf 'status: passed\n' > .flowcordia/automation/studio-v2-activepieces-host.txt
            git add pnpm-lock.yaml .flowcordia/automation/studio-v2-activepieces-host.txt
            git commit -m "Verify genuine Activepieces Studio host" || true
          else
            { printf 'status: failed\n\n'; tail -n 300 /tmp/flowcordia-activepieces-host.log; } > .flowcordia/automation/studio-v2-activepieces-host.txt
            git add .flowcordia/automation/studio-v2-activepieces-host.txt
            git commit -m "Record Activepieces Studio host failure" || true
          fi
          git pull --rebase origin agent/studio-v2-activepieces-frontend
          git push origin HEAD:agent/studio-v2-activepieces-frontend
          exit "$status"
