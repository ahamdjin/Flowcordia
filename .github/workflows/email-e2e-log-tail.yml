name: Email E2E log tail

on:
  pull_request:
    branches: [main]

permissions:
  actions: read
  contents: write

jobs:
  capture:
    if: >-
      github.event.pull_request.head.repo.full_name == github.repository &&
      github.head_ref == 'agent/self-host-email-configuration'
    runs-on: ubuntu-latest
    steps:
      - name: Checkout branch
        uses: actions/checkout@de0fac2e4500dabe0009e67214ff5f5447ce83dd
        with:
          ref: ${{ github.head_ref }}
          persist-credentials: true

      - name: Capture failed E2E tail
        env:
          GH_TOKEN: ${{ github.token }}
        shell: bash
        run: |
          set -euo pipefail
          curl --fail --location \
            -H "Authorization: Bearer ${GH_TOKEN}" \
            -H "Accept: application/vnd.github+json" \
            -H "X-GitHub-Api-Version: 2022-11-28" \
            "https://api.github.com/repos/${GITHUB_REPOSITORY}/actions/jobs/90331156960/logs" \
            --output /tmp/e2e-job.log
          mkdir -p .agent
          {
            echo "# E2E failure tail"
            echo
            echo '```text'
            tail -n 350 /tmp/e2e-job.log
            echo '```'
          } > .agent/email-e2e-failure.md

      - name: Commit diagnostic
        shell: bash
        run: |
          git config user.name "Flowcordia CI"
          git config user.email "flowcordia-ci@users.noreply.github.com"
          git add .agent/email-e2e-failure.md
          git commit -m "Capture email E2E failure tail"
          git push origin HEAD:${{ github.head_ref }}
