name: GitHub onboarding rebase

on:
  pull_request:
    branches: [main]

permissions:
  contents: write

jobs:
  rebase:
    if: >-
      github.event.pull_request.head.repo.full_name == github.repository &&
      github.head_ref == 'agent/self-host-github-onboarding'
    runs-on: ubuntu-latest
    steps:
      - name: Checkout onboarding branch
        uses: actions/checkout@de0fac2e4500dabe0009e67214ff5f5447ce83dd
        with:
          ref: agent/self-host-github-onboarding
          fetch-depth: 0

      - name: Rebase onto current main and remove this workflow
        shell: bash
        run: |
          set -euo pipefail
          git config user.name "github-actions[bot]"
          git config user.email "41898282+github-actions[bot]@users.noreply.github.com"
          git fetch origin main
          git rebase origin/main
          git rm .github/workflows/github-onboarding-rebase.yml
          git commit -m "Remove temporary onboarding rebase workflow"
          git push --force-with-lease origin HEAD:agent/self-host-github-onboarding
